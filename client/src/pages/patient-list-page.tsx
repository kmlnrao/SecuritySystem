import { ComingSoonPage } from "@/components/coming-soon-page";

export default function PatientListPage() {
  return (
    <ComingSoonPage 
      title="Patient List" 
      description="Browse and search through all registered patients in the system."
    />
  );
}